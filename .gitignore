# credentials & secrets
.pgpass
*.pgpass
.env

# extracted data (re-pullable; do not commit)
data/
*.parquet
*.csv
# but the crisis taxonomy is a source artifact, not data:
!config/crises.csv

# python
__pycache__/
*.pyc
.venv/
venv/

# extracted/derived data dirs (re-derivable; do not commit)
wrds_sp500_data/
wrds_sp500_data_w120/

# figures (regenerable from the parquet via src/plots.py)
figures/
figures_w120/

# macOS
.DS_Store
