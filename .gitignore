# credentials & secrets
.pgpass
*.pgpass
.env

# extracted data (re-pullable; do not commit)
data/
*.parquet
*.csv
# but the crisis taxonomy is a source artifact, not data:
!config/crises.csv

# python
__pycache__/
*.pyc
.venv/
venv/

# figures (regenerable from the parquet via src/plots.py)
figures/

# macOS
.DS_Store
